server:
  http_listen_port: 9080
  grpc_listen_port: 0

positions:
  filename: /tmp/positions.yaml

clients:
  - url: http://test-loki-1:3100/loki/api/v1/push

scrape_configs:
  - job_name: flog_scrape 
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 5s
        filters:
          - name: name
            values: ["test-flog-1"]
    relabel_configs:
      - source_labels: ['__meta_docker_container_name']
        regex: '/(.*)'
        target_label: 'container'
    pipeline_stages:
      - match:
          selector: '{container="test-flog-1"}'
          stages:
          - json:
              expressions:
                ts:
                method:
                protocol:
          - labels:
              method:
              protocol:
          - timestamp:
              source: ts
              format: RFC3339Nano
              